; Minimum release age: only allow packages that are at least 7 days old
min-release-age=604800

; Use lockfile-only mode for installs
prefer-offline=true

; Strict SSL
strict-ssl=true

; Save exact versions
save-exact=true
